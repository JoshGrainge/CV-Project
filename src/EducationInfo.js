function EducationInfo(name, location, diploma, start, end) {
  return {
    name: name,
    location: location,
    diploma: diploma,
    start: start,
    end: end,
  };
}

export default EducationInfo;
