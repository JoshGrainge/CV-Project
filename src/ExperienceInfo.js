function ExperienceInfo(title, start, end, company, location, descriptions) {
  return {
    title: title,
    start: start,
    end: end,
    company: company,
    location: location,
    descriptions: descriptions,
  };
}

export default ExperienceInfo;
